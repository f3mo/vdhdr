from setuptools import setup, find_packages

setup(
    name='vdhdr',
    py_modules=['vdhdr'],
    version='0.1',
    packages=find_packages(), 
    author='https://github.com/f3mo',
    description='vdhdr module determines the type of image contained in a file or byte stream.',
    )
